var should = require("should");
var sinon = require("sinon");
var util = require("util");

var log = require("../../red/log");

describe("ubicall/plist/", function() {
  it('should load kb for ubicall', function() {
    
  });
});